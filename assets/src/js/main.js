/*
 * Main
 */

(() => {

	// Web app
	const Web = {
		// Init
		init: () => {
			Web.bind();
		},
		// Bind
		bind: () => {
			// Web.nav.toggle();
		},
		// Global navigation
		// nav: {
		// 	// Toggle
		// 	toggle: () => {
		// 		$('#nav-global-toggle').on('click', () => {
		// 			$('#nav-global-modal').toggleClass('active');
		// 		});
		// 	}
		// }
	};

	Web.init();

})();
